# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key

# Rendered page images (regenerate with render.py)
pages/
pages_sur/

# Source PDFs (public RRC filings, but keep the repo light)
*.pdf

# Model cache
hf-cache/

# Python
__pycache__/
*.pyc
.venv*/
